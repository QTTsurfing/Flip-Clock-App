# PROGRESS.md

## 开工回执（任务 0，2026-08-04）
- 目标：免费 iPad 翻页时钟（PWA，加到主屏幕全屏），仿 Fliqlo 六项功能＋防息屏，零依赖、离线可用。
- 顺序：任务 0 环境核对 → 1 骨架＋时钟引擎＋测试 → 2 设置＋常亮 → 3 PWA → 4 README → 全量验收。
- 最大风险：翻页动画观感（半托，领导亲验）；离线验证需要本地服务＋Chrome 无头截图；本地端口监听需提权。
- 环境实测：node v22.13.1、python3 3.9.6、git 2.50.1；python3 -m http.server 返回 200；flip-clock-app 原本不存在；Chrome 可用。

## 任务 1 记录
- test.js 基线 sha256（2026-08-04）：7374900c44cf046f8dd17a3f844f8fe55bbd30045ca4215d656487677c3f5b36
- 测试基线数：13（跳过 0）。
- 修正记录：task1 首跑 12 通过 1 失败——失败为测试期望笔误（13:00 的 12 小时制应为 01:00），修正期望后 13 全绿；判定标准 test.js 此后不再改动。
- 任务 1 验收：node test.js 全绿；ls -R 文件齐全（icons 下 PNG 在任务 3 生成）。

## 任务 2 记录（2026-08-04）
- 设置面板完成：12/24h、秒显（点屏幕切换）、亮度 20–100%、挡板开关、尺寸 50–150%，localStorage 持久化。
- 修复真实 bug：顶板数字一直为空——updateCard 用 DOM 文本判重，被 250ms 走时循环反复清除 620ms 回填定时器，动画永远卡半路；改为独立 last 状态变量 + 首帧直铺。另补上漏掉的 Service Worker 注册。
- 验收：node test.js 13/13 仍全绿；node verify-settings.js 4 项全过（空存储默认值、保存后读回一致、键名前缀、缺失回落默认）。

## 任务 3 记录（2026-08-04）
- manifest（standalone/黑/双图标）、sw.js（cache-first + 离线回退 index.html）、图标 180/512 PNG（qlmanage+sips 生成）完成。
- 浏览器实测（Chrome 无头 + CDP，localhost:8000）：SW count=1、controller=true；杀掉服务器后离线再开页面仍渲染出 12:05（digits 与截图佐证）。
- 零外链：grep -rn "https\?://" index.html style.css app.js 无输出（退出码 1）。

## 任务 4 记录（2026-08-04）
- README.md 完成：GitHub Pages / 局域网 / 本地双击三种安装、iPad 添加到主屏幕步骤、局限说明、调试参数、验收命令。

## 最终验收记录（2026-08-04）
- 反向验证：故意改错 12 小时制边界 → 2 FAIL、退出码 1；还原 → 13/13 全绿、退出码 0；app.js 与备份逐字节一致。
- 截图证据：12:01 与 12:02 数字不同（像素差异区域 x[748..808] y[344..426]，即分钟个位卡片）；demo 慢速翻页抓到中帧（x[722..854] y[286..474] 全卡区域变化）；竖屏、秒显、离线各一张。
- test.js sha256 与基线一致；BLOCKED.md 无待裁决；全部交付文件位于 flip-clock-app/ 内。

## 修复轮 2（2026-08-05）：面板关不掉 + 卡片质感
- 根因：.settings 的 display:flex 覆盖 hidden 属性 → 面板常显。修复：新增 .settings[hidden]{display:none}（style.css）。
- 卡片观感（按选定方向「复刻 Fliqlo 质感」）：去掉每半一条的重渐变，改顶半 #1c1c1e 微亮 / 底半 #101013 微暗；13px 浓阴影改 2px 细缝 + 4px 极软投影；外圆角保留、缝侧直角。
- 动画：0.4s+0.4s 更舒缓；演示慢速模式同步放大（2s+2s），JS 定时器慢速下改 4400ms（否则会提前截断动画）；新增 prefers-reduced-motion 直接换字不播动画。
- 交互小增强：点设置面板外空白处关闭面板（面板开着时不误触发秒显切换）。
- 浏览器实测：初始隐藏、点齿轮显示、点完成隐藏、点外关闭、设置刷新保留、减弱动态效果无动画类；12:01 vs 12:02 像素差异锁定分钟个位卡片（x[748..808] y[344..426]），翻页中帧 DOM 顶部 1 底部 2；服务器停后离线仍渲染（SW count=1 controller=true）。
- test.js sha256 仍 7374900c44cf046f8dd17a3f844f8fe55bbd30045ca4215d656487677c3f5b36；13/13 全绿；BLOCKED.md 无。

## 修复轮 3（2026-08-05）：翻页黑边
- 根因（像素级排查 + 用户截图确认）：① 卡片之间 flex 间距过大，形成明显黑色竖条；② 旋转层翻动时身后露出纯黑空隙；③ iPad Safari（WebKit）对"圆角+裁切+3D 旋转"元素会渲染黑边。
- 修复（重构翻页结构）：静态圆角外壳（.digit/.card + 静态 .top/.bottom 半片）始终垫底；新增直角、无圆角的旋转翻页层（.fold-top/.fold-bottom），只翻页时显示、带 backface-visibility hidden 与 will-change:transform——旋转时身后永远是外壳深灰，不再露黑，也规避 WebKit 圆角 3D 黑边。
- 卡片间距收紧：unit 间距 9.8px→5.6px，unit 间 15.4px→7.7px（黑色竖条明显变细）。
- 像素验证：翻页中帧卡片区域内近黑像素只剩 1-2px 卡片边缘抗锯齿线（631/29450），不再有 7px 实心黑带；12:01 vs 12:02 差异仍锁定分钟个位卡片（x[736..796] y[344..426]）；离线、面板开/关回归通过；test.js 13/13 全绿、sha256 不变。

## 修复轮 4（2026-08-09）：翻页延迟
- 根因：① 走时用 250ms 轮询，分钟/秒边界后最坏晚 250ms 才检测到翻页；② 旧数字在上半片滞留到动画结束（0.8s）；③ 动画总时长偏长。
- 修复：走时改为精确对齐边界——秒显每秒、否则每分，边界后约 15ms 触发（实测 flipOffsetsMs=[20,19,17,17]、间隔 999-1000ms）；翻页结构改为静态半片即时切到新值、旧值由翻下的挡板带走（翻落过程自然露出新数字）；动画缩短为 0.25s+0.25s（总 0.5s）。
- 验证：边界偏差 17-20ms（修复前最坏 250ms）；12:01 vs 12:02 差异仍锁定分钟个位卡片；翻页中帧 foldNums="12"（挡板带旧值 1、静态已新值 2）；黑边无回归；离线与面板开/关通过；test.js 13/13 全绿、sha256 不变。

## 审查轮（2026-08-29）：查漏补缺
- 逐项核对原版功能覆盖（12/24h、横竖屏、点屏秒显、变暗、挡板开关、尺寸缩放、防息屏）与代码质量，发现并修复：
  1. 防息屏失败提示原先写在隐藏的设置面板内，用户看不到 → 改为独立右下角提示气泡（实测失败时 shown=true）。
  2. 缺 favicon（控制台 404）→ index.html 加 icon 链接。
  3. 无障碍：无键盘焦点样式、开关按钮无 aria-pressed、时钟无读屏标签 → 补 :focus-visible、aria-pressed、clock aria-label（实测 "当前时间 12:01"）。
  4. 删除死代码 pad2。
  5. Service Worker 缓存名 v1→v2，并在 README 增加"更新时版本号 +1"说明，避免旧设备读旧缓存。
- 验证：node test.js 13/13 全绿、sha256 不变、零外链；渲染实测 digits/aria/favicon/竖屏秒显均正常。

## 视觉模型审查轮（2026-08-29）：可优化点
- 接入视觉模型（deepseek-v4-flash-vision-exp）后逐屏目检：默认横屏、竖屏秒显、设置面板、隐藏挡板、变暗、24 小时制、翻页中帧、尺寸 150%。
- 结论：整体扎实——跨缝翻页、配色、圆角、各模式均正确。可优化点：
  1. 右下角设置齿轮太淡（视觉易忽略）→ 已调亮（背景/描边/图标对比提升），仍保持极简。
  2. 12 小时制数字"1"为系统 Helvetica 字形、较朴素；改专属字体需引入外部字体（与零依赖冲突），标注为可选项。
  3. 设置标签"显示秒（点屏幕也可切换）"略长，可精简。
  4. 翻页瞬间直角挡板极小幅度超出圆角轮廓（约 0.1s 内，肉眼几乎不可见），可接受。
- 验证：修改后 node test.js 13/13 全绿、设置持久化通过；齿轮透明度提升后重截确认可见。
